//Crie três variáveis a = 5, b = 3, c = 2.
//Calcule a expressão (a + b) * c - 4 / 2 e exiba o resultado.

function resposta7() {
    const a = 5;
    const b = 3;
    const c = 2;

    const resultado = (a + b) * c - 4 / 2;
    return resultado;
}

export default resposta7;